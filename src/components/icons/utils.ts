import { 
    MoveRight,
    MoveDown,
    Scale,
    Clock,
    Target,
    Brain,
    Layers,
    Waypoints,
    ArrowLeft,
    GalleryVerticalEnd,
    FlaskConical
} from "lucide-react"
import { GithubIcon } from "./custom"


export const LUCIDE_ICON_MAP = {
    MoveRight,
    MoveDown,
    ArrowLeft,
    Clock,
    Scale,
    Target,
    Brain,
    Layers,
    Waypoints,
    GalleryVerticalEnd,
    FlaskConical
}

export const CUSTOM_ICON_MAP = {
    github: GithubIcon
}